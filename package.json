{
  "name": "sentry-auto-fix",
  "displayName": "Sentry Auto Fix",
  "description": "View Sentry issues in Cursor/VSCode and generate AI-powered fix prompts with full context",
  "version": "0.0.2",
  "publisher": "rseyferth",
  "repository": {
    "type": "git",
    "url": "https://github.com/rseyferth/sentry-auto-pr-vscode"
  },
  "keywords": [
    "sentry",
    "error tracking",
    "debugging",
    "ai",
    "cursor",
    "bug fix",
    "mcp"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other",
    "Debuggers",
    "Linters"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "bin": {
    "sentry-mcp": "./dist/mcp-entry.js"
  },
  "contributes": {
    "configuration": {
      "title": "Sentry Auto Fix",
      "properties": {
        "sentryAutoFix.sentryUrl": {
          "type": "string",
          "default": "",
          "description": "Base URL for your self-hosted Sentry instance (e.g., https://sentry.example.com)"
        },
        "sentryAutoFix.apiToken": {
          "type": "string",
          "default": "",
          "description": "Sentry API authentication token"
        },
        "sentryAutoFix.projectSlugs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Array of project slugs to monitor (format: 'organization/project')"
        },
        "sentryAutoFix.clickupApiToken": {
          "type": "string",
          "default": "",
          "description": "ClickUp API token for integration (optional)"
        },
        "sentryAutoFix.clickupTeamId": {
          "type": "string",
          "default": "",
          "description": "ClickUp team ID for integration (optional)"
        },
        "sentryAutoFix.clickupCustomFields": {
          "type": "object",
          "default": {},
          "description": "Custom fields to add to ClickUp tasks (e.g., {\"Type\": \"Bug\"})"
        },
        "sentryAutoFix.clickupSelectedList": {
          "type": "string",
          "default": "",
          "description": "Selected ClickUp list ID"
        },
        "sentryAutoFix.clickupCompletedStatus": {
          "type": "string",
          "default": "complete",
          "description": "Status name to use when marking ClickUp tasks as completed"
        },
        "sentryAutoFix.clickupLanguage": {
          "type": "string",
          "default": "English",
          "description": "Language for ClickUp comments (e.g., 'English', 'Dutch')"
        },
        "sentryAutoFix.customPromptInstructions": {
          "type": "string",
          "default": "",
          "description": "Custom instructions to add to the AI fix prompt (optional)"
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "sentry-issues",
          "title": "Sentry Issues",
          "icon": "$(bug)"
        }
      ]
    },
    "views": {
      "sentry-issues": [
        {
          "id": "sentryIssuesView",
          "name": "Sentry Issues",
          "type": "webview"
        }
      ]
    },
    "commands": [
      {
        "command": "sentryAutoFix.refreshIssues",
        "title": "Refresh Issues",
        "icon": "$(refresh)"
      },
      {
        "command": "sentryAutoFix.fixWithAI",
        "title": "Fix this issue using AI"
      },
      {
        "command": "sentryAutoFix.openIssueInBrowser",
        "title": "Open Issue in Sentry"
      },
      {
        "command": "sentryAutoFix.openSettings",
        "title": "Open Settings",
        "icon": "$(settings-gear)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "sentryAutoFix.openSettings",
          "when": "view == sentryIssuesView",
          "group": "navigation",
          "icon": "$(settings-gear)"
        },
        {
          "command": "sentryAutoFix.refreshIssues",
          "when": "view == sentryIssuesView",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "sentryAutoFix.fixWithAI",
          "when": "view == sentryIssuesView && viewItem == issue"
        },
        {
          "command": "sentryAutoFix.openIssueInBrowser",
          "when": "view == sentryIssuesView && viewItem == issue"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "@types/react": "^18.3.26",
    "@types/react-dom": "^18.3.7",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.x",
    "@typescript-eslint/parser": "^6.x",
    "esbuild": "^0.19.0",
    "eslint": "^8.x",
    "remark": "^15.0.1",
    "remark-parse": "^11.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "axios": "^1.6.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "unified": "^11.0.5",
    "zustand": "^4.5.7"
  }
}
